const Paragraph: string = 'p'

const Image: string = 'img'

const QuickMeaning: string = 'qm'

const ElementTypes = Object.freeze({
    Paragraph,
    Image,
    QuickMeaning
})

export default ElementTypes
